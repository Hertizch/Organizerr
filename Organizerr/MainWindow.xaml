<Window x:Class="Organizerr.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Organizerr.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:BaseViewModel, IsDesignTimeCreatable=True}"
        DataContext="{StaticResource BaseViewModel}"
        Title="MainWindow" Height="950" Width="1600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Content="Radarr" Command="{Binding Path=ChangePageViewModelCommand}" CommandParameter="{Binding Path=PageViewModels[0]}" Padding="4" />
            <Button Content="Sonarr" Command="{Binding Path=ChangePageViewModelCommand}" CommandParameter="{Binding Path=PageViewModels[1]}" Padding="4" />
        </StackPanel>

        <ContentControl Grid.Row="1" Content="{Binding Path=SelectedPageViewModel}" />
    </Grid>
</Window>
